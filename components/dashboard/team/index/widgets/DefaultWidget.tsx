import Card from "@mui/material/Card"
import CardHeader from "@mui/material/CardHeader"

const DefaultWidget = () => {
    return (
        <Card>
            <CardHeader title="Default Widget" subheader="Invalid/Unknown widget" />
        </Card>
    )
}

export default DefaultWidget