import { Card, CardContent, Typography } from "@mui/material";
import { Draggable } from "react-beautiful-dnd";

import { Item } from "./Board";

type Props = {
    item: Item,
    index: number
}

const BoardCard = ({item, index}: Props) => {
  return (
    <></>
  );
};

export default BoardCard;
